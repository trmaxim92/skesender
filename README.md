# SkySender

CRM для переписки с клиентами (аналог Wazzup): FastAPI + Vue 3 + Tailwind.

- **backend/** — API, WebSocket, интеграции MAX / Telegram
- **frontend/** — кабинет оператора

Скопируйте `backend/.env.example` → `backend/.env` перед запуском. Секреты и `.env` в репозиторий не попадают.
